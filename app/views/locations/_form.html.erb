<%= form_for @location do |f| %>
  <% if @location.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@location.errors.count, "error") %> prohibited
      this location from being saved:</h2>
    <ul>
    <% @location.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
<table>
<tr>
    <td><%= f.label :name %></td>
    <td><%= f.text_field :name %></td></tr>
    <tr><td><%= f.label "Primary" %></td>
    <td><%= f.check_box :available %></td></tr>
    <tr><td><%= f.label :active %></td>
      <td><%= f.check_box :active, checked:true %></td></tr>
</table>
  </p>

 
  <p>
    <%= f.submit %>
  </p>
<% end %>