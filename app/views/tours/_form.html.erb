<%= form_for(@tour) do |f| %>
  <% if @tour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tour.errors.count, "error") %> prohibited this tour from being saved:</h2>

      <ul>
      <% @tour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
<%= f.fields_for :shows do|show| %>
      <%= render 'show_fields', :f => show %><br>
<% end %><br>
<div>
      <%= link_to_add_association 'add show', f, :shows %>
</div><br>
  <div class="field">
    <%= f.submit class:"btn btn-primary"%>
  </div>
<% end %>

