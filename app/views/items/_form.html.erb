<%= form_for @item do |f| %>
  <% if @item.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@item.errors.count, "error") %> prohibited
      this item from being saved:</h2>
    <ul>
    <% @item.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
<table>
<tr>
    <td><%= f.label :name %></td>
    <td><%= f.text_field :name %></td></tr>
    <tr><td><%= f.label "Sub" %></td>
    <td><%= f.text_field :sub %></td></tr>
    <tr><td><%= f.label :active %></td>
      <td><%= f.check_box :active, checked: true %></td></tr>

</table>
  </p>

 
  <p>
    <%= f.submit %>
  </p>
<% end %>